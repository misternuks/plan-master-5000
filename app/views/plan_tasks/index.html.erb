<div class="p-5 container d-flex flex-column align-items-center justify-content-center">
  <h1>Your Tasks</h1>
  <% @tasks.each do |task| %>
    <%= link_to task.title, plan_task_path(task), class: "btn btn-primary mt-1 shadow w-50" %>
  <% end %>
</div>
